Ќапишите программу, один поток которой каждую секунду отображает на экране данные о времени,
прошедшем от начала сессии, а другой ее поток выводит сообщение каждые 5 секунд.
ѕредусмотрите возможность ежесекундного оповещени€ потока, воспроизвод€щего сообщение,
потоком, отсчитывающим врем€. ќтсчитывать врем€ (использовать sleep) может только один поток
Ќе внос€ изменений в код потока-"хронометра" , добавьте еще один поток, который выводит на

экран другое сообщение каждые 7 секунд. ѕредполагаетс€ использование методов wait(),
notifyAll() по какому-то монитору, общему дл€ всех потоков. 

Ѕонус: ѕакет Concurrent, перевод примеров на Lock  вместо синхронизации через wait-notify